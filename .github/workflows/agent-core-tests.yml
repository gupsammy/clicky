name: Agent Core Tests

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test Codex app-server core
        run: swift test -Xswiftc -warnings-as-errors
